import { TrashListContainer } from "@/widgets/trash-list";

/**
 * 휴지통 페이지
 *
 * 삭제된 게시글들을 확인하고 관리할 수 있는 페이지입니다.
 * 복원 및 영구 삭제 기능을 제공합니다.
 */
export default function TrashPage() {
  return <TrashListContainer />;
}
