import { MustReadListContainer } from "@/widgets/must-read-list";

/**
 * 필독 게시글 페이지
 *
 * 필독으로 지정된 게시글들을 확인하고 읽음 상태를 관리할 수 있는 페이지입니다.
 */
export default function MustReadPage() {
  return <MustReadListContainer />;
}
